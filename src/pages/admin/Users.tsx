import ScaffoldPage from '@/components/ScaffoldPage';

export default function UsersPage() {
  return (
    <ScaffoldPage 
      title="Users & Roles"
      description="Manage user access and permissions (Owner, Manager, Tech, Read-only)."
    />
  );
}